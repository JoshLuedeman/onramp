# docker-compose.override.yml — Dev overrides (auto-loaded)
version: "3.9"

services:
  backend:
    # Override to use uvicorn with reload for development
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    volumes:
      - ./backend/app:/app/app
